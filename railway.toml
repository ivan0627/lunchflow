# railway.toml


# Scripts del backend
[scripts]
build_backend = "npm install && npm run build"
run_backend = "nodemon index.js"

# Dependencias del frontend en React
[frontend]
react = "17.x"

# Scripts del frontend
[frontend.scripts]
build_frontend = "npm install && npm run build"
run_frontend = "npm start"